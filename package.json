{
  "name": "example",
  "private": true,
  "scripts": {
    "compile": "tsc -b",
    "clean": "rm -rf tsconfig.tsbuildinfo packages/*/dist",
    "server": "ts-node --respawn --no-notify packages/local/index.ts"
  },
  "devDependencies": {
    "@pulumi/aws": "^5.0.0",
    "@pulumi/pulumi": "^3.0.0",
    "@types/node": "^12.0.0",
    "ts-node": "^7.0.1",
    "typescript": "~3.8.3"
  },
  "workspaces": ["packages/*"]
}
